{
  "name": "routecare-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:dispatcher": "npm run dev --workspace=dispatcher",
    "dev:driver": "npm run dev --workspace=driver",
    "build": "npm run build --workspaces",
    "type-check": "npm run type-check --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
